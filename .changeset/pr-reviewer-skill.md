---
'@agentskit/skills': minor
---

feat(skills): add `prReviewer` — reviews a diff against the AgentsKit Manifesto + package CONVENTIONS, flags violations (new core deps, `any`, default exports, hardcoded styles, dead re-exports, narrating comments), and suggests concrete rewrites.
